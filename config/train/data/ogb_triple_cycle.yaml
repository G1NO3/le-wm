dataset:
  name: ogbench/cube_triple_stochastic_cycle
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  keys_to_load: [pixels, action, observation]
  keys_to_cache: [action, observation]
split_fractions: [0.8, 0.1, 0.1]
split_manifest: null
