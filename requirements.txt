# Pip-compatible direct dependencies for local development.
#
# Python 3.10 is required. Pixi remains the reproducible source of truth:
# use `pixi install --locked` when reproducing experiments or cluster jobs.

stable-worldmodel[train,format]==0.1.1
stable-pretraining==0.1.8
datasets==2.21.0
huggingface-hub>=1.2,<2
torch>=2.4,<3
torchvision

# Environment and dataset support
ogbench==1.2.1
gymnasium-robotics==1.4.2
imageio>=2.37,<3
pygame>=2.6,<3
pymunk>=7.1,<8
shapely>=2.1,<3

# Development and validation
pytest>=8,<9
